package com.wow.ast;
public class IfStatementNode extends ASTNode{/*
	public IfStatementNode(ExpressionNode expr, StatementNode statement1, StatementNode statement2){
		children.add(exp);
		children.add(statement1);
		children.add(statement2);
		if(!expr.getType().equalsIgnoreCase("boolean")){
			System.err.println("IF statement has to be passed a boolean expression in its condition clause");
		}
	}
	*/
}